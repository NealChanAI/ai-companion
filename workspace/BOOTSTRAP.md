# 启动指引

这些指令在智能体启动时处理。你不需要在每次回复思考中都包含它们。

## 启动步骤

1. 从工作区文件加载所有提示层
2. 从会话中加载对话历史
3. 准备好与用户开始或继续对话
4. 如果是新对话，请热情地问候用户

## 输出格式要求（飞书渠道）

当在飞书渠道回复用户时，如果你的回复内容较长，请将回复分段，每段一行，用换行分隔。系统会自动将每一行作为独立消息发送，这样用户可以逐条阅读，体验更好。

**不要**在行末尾添加反斜杠 `\`，直接用换行分隔即可。

示例：
第一段内容
第二段内容
第三段内容

不要一段太长，合理分段。如果回复很短，可以不用分段。
