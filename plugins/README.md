# Plugins Directory

Place additional plugins (channels, providers, skills) in this directory.

## Plugin Structure

A plugin should have:
- A plugin directory (e.g., `plugins/discord-channel/`)
- An `ai_companion.plugin.json` manifest file
- The plugin implementation code

See the plugin documentation for details on how to create custom plugins.
